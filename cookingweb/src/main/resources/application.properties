# === 1. Cấu hình kết nối MySQL ===
# Sửa 3 dòng dưới đây cho đúng với MySQL của bạn
spring.datasource.url=jdbc:mysql://localhost:3306/db_cookingweb?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Dung@3101

# (Bạn không cần sửa các dòng dưới này)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === 2. Cấu hình JPA (Hibernate) ===
# Giúp tự động tạo/cập nhật bảng trong CSDL khi bạn viết code Entity
spring.jpa.hibernate.ddl-auto=update

# Hiển thị các câu lệnh SQL trong cửa sổ terminal (tốt để debug)
spring.jpa.show-sql=true

# === 3. Cấu hình Server ===
# Chạy backend trên cổng 8080
server.port=8080

# Cấu hình Google Gemini AI
# Cấu hình Gemini AI
# (Dán mã key bạn vừa gửi vào đây)
gemini.api.key=AIzaSyCX25PxRvtSi_zYctUZYwuyFWPkp5TPzVk
# Dùng Gemini 1.5 Flash (Bản mới nhất, hỗ trợ v1beta)
# Dùng phiên bản flash mới nhất (thêm chữ -latest)
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent